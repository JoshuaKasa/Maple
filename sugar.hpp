#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <algorithm>
#include <random>
#include <chrono>
#include <map>
#include <cstdint>

namespace sugar {
int16_t sugar(int32_t n) {
std::cout << n << std::endl;
}
int16_t hello(int32_t n) {
std::cout << n << std::endl;
}
void test(int32_t n) {
std::cout << 1 << std::endl;
}
}
int main() {
std::map<std::string, int8_t> backups;

std::cin.get();
return 0;
}
