#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <algorithm>
#include <random>
#include <chrono>
#include <map>
#include <cstdint>

int16_t test(int16_t p1, int16_t p2, int16_t p3) {
std::cout << 1200 << std::endl;
return p1;
}
int main() {
std::map<std::string, int8_t> backups;
for (int run = 0; run < 5; run++) {
const int8_t hello = 3;
const int16_t var = test(1, 2, 3);
}
test(1, 2, 3);
std::cout << test << std::endl;

std::cin.get();
return 0;
}